input_string = input("Введите строку: ")
output_string = ""

for char in input_string:
    if "a" <= char <= "z":
        output_string += chr(ord(char) - 32)
    else:
        output_string += char

print("Результат: ", output_string)
hello world

# По коду вопросов нет. Но!
# 1. Перевести этот файл в расширение .py
# 2. Оформить решение этой задачи в виде функции.
# 3. Решение записать в отдельного файла и передать через git
